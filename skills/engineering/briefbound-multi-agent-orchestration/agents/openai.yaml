interface:
  display_name: "Briefbound 多会话 Agent 协作"
  short_description: "通过受控 relay 让同项目平级会话交换证据、依赖并降低集成冲突"
  default_prompt: "使用 $briefbound-multi-agent-orchestration 通过 BRT_TRUSTED_RELAY_V1 协调同项目现有平级会话；每个会话保留原任务和权限，只发送会改变行动的证据、依赖、冲突与集成消息。"
